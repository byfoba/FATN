# Lessons Learned

- Start every non-trivial task with a written checklist and verification plan.
- Keep API contracts centralized in shared types to avoid drift between bridge, API, and tests.
- Validate model output schema strictly before persistence.
